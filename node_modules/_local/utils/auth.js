debug = require('debug')('auth');
module.exports = {
  isAdmin: function(req, res, next) {
    debug(req.isAuthenticated(), req.user);
    if(req.isAuthenticated() && req.user.type === 'admin') {
      next();
    } else {
      next(new Error('Not Admin'));
    }
  }

}
