/*
A small util to make tracing through code much easier,
also a marker that a file needs to be cleaned up, as this shouldn't be used for
production code.

*/

//font list: http://www.flamingtext.com/tools/figlet/fontlist.html
var figlet = require('figlet');
var colors = require('_local/utils/colors');
module.exports =function (text, font) {
  font = typeof font === 'undefined' ? 'Standard' : font;
  output = colors.rainbow(figlet.textSync(text, { font: font }));
  process.stdout.write( output + '\n');
};
