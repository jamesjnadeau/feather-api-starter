//load the env
require('dotenv').load();
console.log('sanitizedEnv', env);
var env = process.env;
var sanitizedENV = {
  API_URL: env.API_URL,
  STAGING_URL: env.STAGING_URL,
  NODE_ENV: env.NODE_ENV,
};

module.exports = sanitizedENV;
