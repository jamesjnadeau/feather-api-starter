//load the env
require('dotenv').load();
var env = process.env;
var sanitizedENV = {
  API_URL: env.API_URL,
  STAGING_URL: env.STAGING_URL,
  FRONTEND_URL: env.FRONTEND_URL,
  NODE_ENV: env.NODE_ENV,
  STORAGE_URL: 'https://'+env.AWS_STORAGE_BUCKET+'.s3.amazonaws.com/',
};

module.exports = sanitizedENV;
