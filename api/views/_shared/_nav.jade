nav.navbar.navbar-inverse.navbar-fixed-top
  .container
    .navbar-header
      button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar', aria-expanded='false', aria-controls='navbar')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/') API and Backend for G.I. Go Fund
    .navbar-collapse.collapse
      ul.nav.navbar-nav.navbar-right
        if(!req.isAuthenticated())
          li.active
            a(href='#{env.FRONTEND_URL}') Return To Site 
          li
            a(href='/auth/login') Login
        else
          li.active
            a(href='#{env.FRONTEND_URL}') Frontend
          li
            a(href='#{env.STAGING_URL}' target="_blank") Staging
          li.dropdown
            a.dropdown-toggle(href='#', data-toggle='dropdown', role='button', aria-haspopup='true', aria-expanded='false')
              | Admin #{req.user.username} 
              span.caret
            ul.dropdown-menu
              li.dropdown-header Content
              li
                a(href='/content/create/news') News
              li
                a(href='/content/create/event') Event
              li.divider(role='separator')
              li.dropdown-header Advanced
              li
                a(href='/admin/jsoneditor') JSON Admin
              li.divider(role='separator')
              li
                a(href='/auth/logout') Logout
